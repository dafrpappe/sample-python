pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        sh '''
          sudo apt-get install git
          git --version
          curl --version
        '''
      }
    }
  }
}
