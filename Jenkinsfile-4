pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        sh '''
          apt-get install git
          git --version
          curl --version
        '''
      }
    }
  }
}
